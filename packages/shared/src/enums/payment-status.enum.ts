export enum PaymentStatus {
  Created = 'created',
  HoldCreated = 'hold_created',
  Authorized = 'authorized',
  Finalized = 'finalized',
  Cancelled = 'cancelled',
  Expired = 'expired',
  Failed = 'failed',
  Refunded = 'refunded',
}
