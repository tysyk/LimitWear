export enum RequestStatus {
  Open = 'open',
  InReview = 'in_review',
  Approved = 'approved',
  Rejected = 'rejected',
  NeedsChanges = 'needs_changes',
  Resolved = 'resolved',
  Cancelled = 'cancelled',
}
