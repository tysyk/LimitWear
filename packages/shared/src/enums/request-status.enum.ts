export enum RequestStatus {
  Submitted = 'submitted',
  UnderReview = 'under_review',
  NeedsChanges = 'needs_changes',
  Approved = 'approved',
  Rejected = 'rejected',
  Completed = 'completed',
  Cancelled = 'cancelled',
  Archived = 'archived',
}
