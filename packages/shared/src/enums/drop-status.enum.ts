export enum DropStatus {
  Draft = 'draft',
  Scheduled = 'scheduled',
  Active = 'active',
  Successful = 'successful',
  Failed = 'failed',
  InProduction = 'in_production',
  Shipped = 'shipped',
  Completed = 'completed',
  Cancelled = 'cancelled',
}
